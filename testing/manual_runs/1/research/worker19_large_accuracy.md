# Worker 19 — large-N accuracy adversary

Date: 2026-08-28
Scope: current `src/dht_asym.c`, `N=65536`, block ratio 4, one FFTW
worker, and only `(K,z0)=(10,30)`, `(11,23)`, `(12,21)`.

## Status

**Overall: FAIL / INCOMPLETE — no profile is certified.** The independent
400-bit MPFR run was intentionally stopped after 250.44 seconds at the user's
request. It completed the three common deterministic inputs below, but did not
reach the `chirp` input or any residual-sign-aligned case. The completed cases
all pass the selected-row gates; that is useful margin evidence, not a full
arbitrary-vector acceptance result.

Provisional ranking from completed evidence:

1. `K12,z0=21`: best accuracy/work tradeoff observed.
2. `K10,z0=30`: largest completed accuracy margin, but the most direct work.
3. `K11,z0=23`: weakest completed accuracy margin; do not promote without the
   omitted adversarial cases.

## Independent checker

Checker source: [worker19_large_accuracy.c](/Users/alex/areas/productive/orchestration/testing/manual_runs/1/research/worker19_large_accuracy.c)

The checker evaluates every selected row with `mpfr_j0` at 400-bit precision,
accumulates the complex dot product at 400 bits, and rounds the reference to
binary64 before comparison. The input values are binary64 values generated by
fixed formulas. The 49 selected rows contain every ratio-4 block-edge
neighborhood, neighborhoods around the profile-specific first active rows,
`m=32768` and its neighbors, and 13 fixed deterministic random-looking rows.
The first active rows are 16 for `K10,z0=30` and 4 for both other profiles.

The project gates used were normalized L2 `<=1e-13` and scaled Linf
`<=1e-12`, where scaled Linf is `max row error / max row reference magnitude`
over the selected rows.

## Reproduction commands

Run from the repository root:

```sh
mkdir -p /private/tmp/worker19_large_accuracy
clang -O3 -DNDEBUG -std=c11 -Wall -Wextra -Wpedantic -I/opt/homebrew/include \
  -c src/dht_asym.c -o /private/tmp/worker19_large_accuracy/dht_asym.o
clang -O3 -DNDEBUG -std=c11 -Wall -Wextra -Wpedantic -I/opt/homebrew/include \
  research/worker19_large_accuracy.c \
  /private/tmp/worker19_large_accuracy/dht_asym.o \
  -L/opt/homebrew/lib -lmpfr -lgmp -lfftw3_threads -lfftw3 -lm \
  -o research/worker19_large_accuracy
/usr/bin/time -p research/worker19_large_accuracy \
  > /private/tmp/worker19_large_accuracy/run.log 2>&1
```

Environment: Apple clang 21.0.0, MPFR 4.2.2, GMP 6.3.0, FFTW 3.3.10_3.
The source hash observed immediately before compilation was
`4c56a13679f92341ef358f3ab9bea57dfb774fb07429ca026d120336216220b7`.
The on-disk `src/dht_asym.c` changed afterward (later observed hash
`1a062beb2b772458613111147b925dd07e5dc3b28e0bb90a2c149bf311156731`), so
the results must not be treated as validation of that later source state.
The run exited 130 because it was interrupted; no files under `src/` or
`bench/` were edited.

## Completed results

All rows below are selected-row comparisons. Each case gate is `PASS`; the
missing cases keep the profile-level status `INCOMPLETE`.

| case | K10/z0 rel-L2 / scaled-Linf | K11/z0 rel-L2 / scaled-Linf | K12/z0 rel-L2 / scaled-Linf |
|---|---:|---:|---:|
| random | `2.263796470116540e-15 / 2.263933652680670e-15` | `1.359789783045323e-14 / 2.062585828810853e-14` | `6.193786645969611e-15 / 8.178230470694642e-15` |
| alternating | `4.789331772325482e-16 / 4.815474730674444e-16` | `4.375834076709406e-16 / 2.198007245961899e-16` | `3.272509771646647e-16 / 1.141653979710101e-16` |
| high_dynamic | `4.227842977020306e-15 / 8.390958148569461e-15` | `1.049069106099133e-14 / 2.393704967720604e-14` | `9.295350566856474e-15 / 1.621670421464423e-14` |

Maximum completed errors and profile status:

| profile | direct entries | plan bytes | max completed rel-L2 | max completed scaled-Linf | case / row for max Linf | status |
|---|---:|---:|---:|---:|---|---|
| `K10,z0=30` | 6,603,633 | 74,849,360 | `4.227842977020306e-15` | `8.390958148569461e-15` | high_dynamic / 256 | **PASS on completed cases; INCOMPLETE overall** |
| `K11,z0=23` | 5,194,869 | 65,676,408 | `1.359789783045323e-14` | `2.393704967720604e-14` | high_dynamic / 17 | **PASS on completed cases; INCOMPLETE overall** |
| `K12,z0=21` | 4,769,025 | 64,366,816 | `9.295350566856474e-15` | `1.621670421464423e-14` | high_dynamic / 64 | **PASS on completed cases; INCOMPLETE overall** |

## Recommendation

Do not call any profile production-safe from this bounded run. If a provisional
choice is required before rerunning the omitted adversarial cases, use
`K12,z0=21` as the front-runner: it uses fewer direct entries than `K11,z0=23`
and its completed maximum scaled Linf is lower. Keep `K10,z0=30` as the
accuracy-margin fallback. The unfinished residual-sign tests are specifically
the cases that can expose a reproducible asymptotic-tail violation, so they are
required before promotion.
